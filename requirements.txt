# Core agent dependencies
anthropic>=0.40.0
tenacity>=8.2.0
pyyaml>=6.0.0
numpy>=1.26.0
scipy>=1.12.0
structlog>=24.1.0

# rclpy is installed via ROS2 apt packages — listed here for documentation only
# gtsam Python bindings are installed during the Docker build from source
